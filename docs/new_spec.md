# To Dos

1. integrate apps as new feature to it like notion, spotify, emails, calenders, or any other apps user wants
2. unify the second brain and current knowledge graph
3. use the unifed second brain and speedup any furture development with less context like rather reading entire codebase make use of second brain and send information of the codebase to the LLM so tokens can be saved.
4. the knowledge graph visualisation in dashboard page it should be a neural network not a sphere like dummy 3d object and the knowledge graph is not updating or stays idle even though i gave root path it is not computing the grpah for root path
5. when i click on the providers in dashboards its not opening any chat section for terminal view to run tasks or simple chat for tasks and update the agents view
6. the theme change button is not doing anything
7. unable to create tasks
8. while creating tasks if i give provider can you suggest available model from provider like some models i might not have access or ranout of tokens per day and all. so think through this